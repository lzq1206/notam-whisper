# Session State

## WAL

- 2026-09-22 result: Published `f4cefbc45058731be5b7fb5486a3627cff787fbd` and Pages workflow `35709104616` completed successfully. Space Devs returned 100 rows; the frontend retained only Hour/Minute/Second precision records (11 coordinate-bearing concrete launches currently), excluding Month/Day/Quarter/Year placeholders such as Ariane 62 | Metop-SG B1 while retaining Soyuz 2.1b | Progress MS-36 (97P). Launch colors are recalculated from the active NOTAM timeline whenever `renderRows()`/expiry refresh runs, so Leaflet markers, Cesium entities, and the popup title stay aligned with the selected range; current-view KML continues to use the same filtered launch subset. Live verification: 5 default-range markers, 1 marker after a single-day Apply action, H3-24 changed from `rgb(162,64,136)` to `rgb(43,91,255)`, the launch popup contained no Launch Library 2 hyperlink/text, and the page reported no JavaScript errors. Regression status: 25 frontend tests passed; the only excluded failure is the pre-existing unrelated OrbitWhisper-link assertion. Backup: `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_launch_color_precision`. Risk: Space Devs precision metadata and external image/feed availability remain network-dependent; records without precision metadata use the narrow-window compatibility fallback.

- 2026-09-22 audit/planned: Refine the future-launch layer in four scoped ways: recalculate launch marker/3D colors from the active NOTAM date range on every render/filter action so the red-to-blue timeline matches NOTAMs; keep launch visibility and summary synchronized with the same Apply/reset action; require a Space Devs `net_precision` that represents a concrete time (Minute/Hour/Second, with a narrow-window fallback only when precision metadata is absent), excluding month/year-only records such as Ariane 62 | Metop-SG B1; and remove only the Launch Library 2 popup hyperlink while retaining source/image metadata. Preserve Space Devs as the primary feed, existing RLL/local fallback, date-range synchronization, and user-owned MSI changes. Backup: `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_launch_color_precision`.

- 2026-09-22 published: Published commit `fe7cc47c9e5e88e55fcf6bb60313bd7a99e7cc3d`; Pages workflow `35706333671` completed successfully. Live verification confirmed 5 upcoming markers and status text under the default 2026-09-22–2026-09-29 NOTAM range, 2 markers for 2026-10-01 only, and 100 markers after clearing the dates; the live page reported no JavaScript errors. Risk: date-filter behavior depends on the browser's current date controls and the external Space Devs feed; no out-of-range launches are rendered in the filtered subset. Next: retain the new range-sync regression test for future launch-feed changes.

- 2026-09-22 result: Upcoming Space Devs/RLL launch visibility now follows the NOTAM `startDate`–`endDate` selection using inclusive UTC day bounds. Leaflet markers, Cesium entities, related-launch text, KML export, timeline bounds, and the launch summary all use the same filtered subset; clearing the dates restores all unexpired launches. Browser verification produced 5 markers for the default 2026-09-22–2026-09-29 range, 2 for 2026-10-01 only, and 100 after reset, with no page errors. Regression status: 25 `test_index_*.js` tests passed; the only failure remains the pre-existing unrelated OrbitWhisper-link assertion. Backup: `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_notam_launch_date_sync`. Next: publish the scoped frontend/WAL/test commit and recheck Pages.

- 2026-09-22 audit/planned: Synchronize upcoming-launch visibility with the NOTAM date controls `startDate`/`endDate`. Use the same inclusive UTC start-of-day/end-of-day range for launch NET timestamps across Leaflet markers, Cesium entities, related-launch popup links, and current-view KML; a blank reset range shows all unexpired upcoming launches. Rebuild the visible launch layer after `applyFilter()` and reset, while preserving Space Devs as the primary source and RLL/local fallback. Backup completed at `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_notam_launch_date_sync`; pre-existing MSI edits remain out of scope.

- 2026-09-22 published: Published commit `34a88f3231ad59e941475f0da1d5c84c6cebd151` on top of remote `ed4331289b72e97863b6bc7fef714ed011890bef`; GitHub Pages workflow `35703227485` completed successfully. Live browser verification at `rocket.rainywhisper.com` reported 100 upcoming markers, an image-bearing Space Devs popup with explicit launch-window start/end, no temperature text, and no page errors. Risk: the API/image resources are external; the RLL/local fallback remains available if the primary request fails. Next: monitor the next scheduled data refresh and recheck API window coverage if Space Devs changes its response.

- 2026-09-22 result: Promoted Launch Library 2 to the primary upcoming-launch source with `mode=normal`, `limit=100`, and strict `window_start`/`window_end`/`net` validation; the primary list currently has 100 concrete, coordinate-bearing records in the live API response. RLL and local schedules remain fallback-only. Removed future-launch temperature output from the popup, KML description, and launch record. Regression status before publication: 24 `test_index_*.js` tests passed; the only failure is the pre-existing unrelated OrbitWhisper-link assertion. Backup: `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_spacedevs_primary`. Risk: the primary API and external images remain network-dependent; if the API fails, the existing RLL/local fallback is used. Next: publish the scoped frontend/WAL/test commit and verify the live page.

- 2026-09-22 audit/planned: Promote Launch Library 2 (`ll.thespacedevs.com/2.3.0/launches/`) from supplemental metadata to the primary upcoming-launch source. Request up to 100 results in `mode=normal`, require concrete `window_start`, `window_end`, and `net`, and keep only records whose launch window has not ended; this will allow more than five timed future launches while respecting the API's published window. RocketLaunch.Live/local schedules remain fallback-only if the primary request fails or yields no qualifying records. Remove temperature from upcoming-launch popup/KML/record rendering. Backup completed at `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_spacedevs_primary`; pre-existing MSI edits remain out of scope.

- 2026-09-22 result: Added optional Launch Library 2 metadata fetch (`mode=list`, `window_end__gte=now`) and matched it to the existing RocketLaunch.Live/local upcoming records. 2D launch popups and 3D Cesium descriptions now render the API image with safe HTTP URL handling, image credit, and the Launch Library 2 detail link when available; launch-window start/end are also displayed. Upcoming filtering and timer expiry now use `window_end`/`win_close` when present and otherwise fall back to the existing RLL/local scheduled time. Browser verification on the local page loaded five current future markers, showed an image in the clicked launch popup, and reported no page errors. Regression status: 24 frontend JavaScript tests passed; the only failure is the pre-existing unrelated OrbitWhisper-link assertion; 9 targeted Python tests passed. Risk: Launch Library 2 is supplemental and failure leaves the RLL/local layer usable; images remain external resources and can be unavailable independently of launch data. Next: commit only the scoped frontend/WAL/test files and push to `origin/main`, excluding pre-existing MSI edits and `backups/`.

- 2026-09-22 audit/planned: Extend the existing future-launch layer with Launch Library 2 (`ll.thespacedevs.com/2.3.0/launches/`) as a supplemental metadata source. Preserve RocketLaunch.Live as the authoritative upcoming feed and retain the local CSV fallback/merge rules. Add per-launch image metadata from the Space Devs `image.image_url`/`thumbnail_url` fields to the 2D popup and 3D description, and change upcoming visibility to use the scheduled window end when available (`window_start`/`window_end`, with current RLL `t0`/`win_open` fallbacks), so a launch remains visible while its window is open but disappears after the end of the window. Backup completed at `/mnt/nas_share/OpenClaw/QA/rocket_backup_20260922_pre_spacedevs`; pre-existing MSI edits remain out of scope.

- 2026-09-20 audit: The live RocketLaunch.Live `/json/launches/next/5` payload now exposes the canonical scheduled time in `t0` (for example `2026-09-20T01:47Z`) while `win_open` is null for most records; the frontend currently reads only `win_open` before parsing the human-readable description. The same payload uses object-form `pad.location` and introduces a `wenchang-satellite-launch-center` slug that is not covered by the current coordinate aliases (the Taiyuan slug is already mapped), so some valid API records have no marker. Planned fix: normalize the RLL time/coordinate schema with `t0` precedence and compatible location aliases, preserve the RLL API-authoritative merge and local fallback behavior, and add a live-payload regression fixture/test before publication.

- 2026-09-20 result: Added `t0`/`win_open`/`net` time normalization, current Wenchang satellite-center coordinate mapping, object/string/direct-coordinate compatibility for RLL pads, and `test_index_rll_schema.js`. Targeted launch tests pass; the full frontend sweep passes 23/24, with only the pre-existing unrelated OrbitWhisper-link assertion failing. Local browser verification loaded six upcoming markers (five current RLL records plus the local Starship fallback), and an RLL StriX Launch 13 popup displayed the correct UTC/local time and mission link with no page errors.

- 2026-09-18 audit: The live local page currently loads `USSF-385 ("R-3")` at `2026-09-26T11:56:00Z` and Starship Orbital Flight 1 / Flight 14 at `2026-09-28T12:15:00Z`. Their solar-side classifier returns the night-side branch, so the remaining launch-specific interaction defect is downstream: the 3D upcoming-launch call passes `launchSolarSelection(launch)` as an ignored second argument to `cesiumViewer.entities.add()`, unlike NOTAM entities which use `addCesiumEntity()` to attach the selection. The 2D launch markers are created in merged API/local feed order, so coincident launch sites do not deterministically put the nearest future launch on top. Planned fix: attach the launch selection through `addCesiumEntity()` and render timed 2D launches from farthest to nearest with an explicit stack rank, preserving RocketLaunch.Live precedence, launch times/coordinates, seven-day NOTAM default, and the existing solar geometry.

- 2026-09-18 result: Routed 3D upcoming launches through `addCesiumEntity()` so their selection state reaches the shared NOTAM/launch solar-overlay handler. Added deterministic 2D ordering: TBD markers first, timed records from farthest to nearest, and `zIndexOffset` increasing with render order, making the closest future launch topmost at overlapping coordinates. Local browser verification selected Starlink (2026-09-20) above the same-site USSF-385 (2026-09-26) in 2D; both USSF-385 and Flight 14 selected in 3D and produced four solar curves with globe lighting. Targeted and full frontend tests pass except the pre-existing unrelated OrbitWhisper-link assertion.

- 2026-09-18 published: Commit `5fe5d85` was pushed to `origin/main`. GitHub Raw and `rocket.rainywhisper.com/index.html?v=5fe5d85` expose the launch-selection and overlap-order fixes; the published page was browser-verified for Starlink-over-USSF-385 2D stacking and Flight 14 3D solar selection. Risk: the unrelated OrbitWhisper-link regression remains pre-existing; CDN verification should use the version query during future deployments. Next: keep the API-authoritative launch merge and recheck stack order after any launch-feed refresh.

- 2026-09-18 audit: Selecting A0095/26 at its current interval midpoint (`2026-09-25T13:35:00Z`) reproduces an inverted 2D twilight mask: locations with positive solar altitude (for example Paris and New York) are shaded, while the NOTAM area at 25S/74E is night. `buildNightMaskRings()` always treats the terminator ring as a daylight hole, but its planar interior changes side with the solar declination under the current unwrapped-longitude representation. Planned fix: classify the ring with the antipodal subsolar point, which is always night, and render either the ring directly or the outer-world-minus-ring mask accordingly; add a regression test without changing NOTAM data, midpoint timing, or 3D solar curves.

- 2026-09-18 result: Added antipodal-subsolar side classification for the 2D solar mask. A0095/26 now renders the direct night ring, while northern-summer cases retain the outer-world-minus-daylight-hole form. The browser click path was verified on the local page with A0095/26 at `2026-09-25T13:35:00Z`: the NOTAM area and other night-side locations darken, while Paris, New York, and Greenwich remain unshaded. Added `test_index_solar_night_side.js`; targeted solar and launch-interaction tests pass. No NOTAM data, midpoint timing, date-line world-copy logic, or 3D curve logic was changed. Risk: the mask still relies on the existing unwrapped lon/lat projection and should be rechecked if the solar path sampling or map renderer changes.

- 2026-09-18 audit: Final geometry review found that anchoring `outerRing` at `daylightBoundary[0][1] + 180` would misalign the outer-minus-hole fallback for northern-summer paths whose unwrapped boundary is centered near the starting longitude. Planned correction: retain the original boundary-mean anchor for the fallback outer ring; the direct-ring branch used by A0095/26 is unaffected.

- 2026-09-18 result: Restored the boundary-mean anchor for `outerRing` and extended the regression test to assert that northern-summer fallback geometry keeps that anchor. The final local frontend sweep passes 21/22 tests; the sole failure is the pre-existing `test_index_orbitwhisper_link.js` assertion, unrelated to this change. GitHub raw and `rocket.rainywhisper.com` both expose the final solar-mask logic.

- 2026-09-18 audit: RocketLaunch.Live's unauthenticated `/json/launches/next/{n}` response ignores `{n}` and returns only five records, ending at USSF-385 on 2026-09-26; the requested Starship Orbital Flight 1 / Starship Flight 14 is scheduled for 2026-09-28 12:15 UTC and is absent from `upcoming_launches.csv`. Planned fix: add the verified RLL mission page as a local fallback schedule entry, leave the original RocketLaunch.Live API request and API-authoritative merge unchanged, and add a regression assertion. The homepage NOTAM default remains seven days.
- 2026-09-18 audit: The anti-meridian renderer currently compacts a crossing polygon into a single longitude range above 180 degrees. Leaflet can normalize the map center to the opposite world copy while the layer remains in the first copy, so the New Zealand/eastern-ocean geometry disappears when panning west of the date line. Planned fix: retain the compact fit copy and render adjacent wrapped copies for 2D Leaflet layers, with dedicated bounds that exclude duplicate copies; preserve canonical KML and Cesium geometry and add a world-copy regression test.
- 2026-09-18 result: Added the Starship Flight 14 local fallback from the verified RocketLaunch.Live mission page (2026-09-28 12:15 UTC, Starbase / Orbital Pad 2) while keeping the RLL API request and API-authoritative merge unchanged. Added adjacent westward Leaflet world copies for anti-meridian geometry and excluded those duplicates from map fitting. Targeted launch/date-line tests pass; the full frontend sweep passes 20/21, with only the pre-existing OrbitWhisper-link assertion failing.

- 2026-09-18 user correction: The initial NOTAM date range must remain seven days; revert only the 30-day default introduced in `003bf49`. Keep the upstream record coverage and anti-meridian rendering fixes unchanged.
- 2026-09-18 result: Restored `endEl.value = plusDays(today, 7)` and updated the regression assertion. The NOTAM source/parser, 30-day backend fetch horizon, requested records, and anti-meridian rendering logic were left unchanged.

- 2026-09-18: Fix planned for missing launch NOTAMs A4456/26, A4457/26, A4631/26, and A4632/26. The upstream `data_dict.json` stores these in `FOCUSED_NOTAM_DATA`, while `_iter_global_supplement_features()` currently reads only `NOTAM_DATA`; extend the parser to cover both sections and add a focused-schema regression test. Preserve the RocketLaunch.Live API and existing NOTAM filters.
- 2026-09-18 result: Extended the Joey supplement parser to read both `FOCUSED_NOTAM_DATA`/`GEOMETRY` and `NOTAM_DATA`/`COORDINATES`. The targeted Python suite passed 36/36, and a live upstream `data_dict.json` parse returned all four requested IDs with four-point polygons. A full local refresh was attempted but the local network returned TLS EOF errors for RocketLaunch.Live and notammap.org, so existing generated outputs were left unchanged for the CI refresh.
- 2026-09-18 published: GitHub Actions run `35260011771` completed successfully after commit `ad2c8af`; generated `notams.csv`, KML, and weekly history now contain A4456/26, A4457/26, A4631/26, and A4632/26. Both `rocket.rainywhisper.com/notams.csv` and the GitHub Pages CSV were verified to expose all four records.

- 2026-09-18 audit: The current Joey `data_dict.json`, local `notams.csv`, and both published CSV endpoints already contain B1847/26 (MMFR), A0095/26 (FIMM), B4913/26 (RPHI), and B4610/26 (NZZO) with parsed polygons. B4913/26 starts on 2026-10-07, beyond the frontend's default seven-day window, while the other two requested records can be obscured by the narrow default view. B4610/26 crosses the anti-meridian; the current renderer unwraps its ring into longitudes below -180 and fits only that world copy, so the New Zealand-side and eastern-side portions cannot be shown together. Planned fix: widen the initial active-NOTAM window to the fetcher's 30-day horizon and split anti-meridian polygons into wrapped, independently renderable segments for 2D, 3D, and KML. Preserve the RocketLaunch.Live API and existing MSI changes.

- 2026-09-18 result: Updated the initial NOTAM date range to 30 days and added anti-meridian polygon/path splitting with compact world-copy rendering for the 2D and Cesium layers, plus canonical split rings for KML. Added regression coverage for the four current spaceflight corridors and the B4610/26 date-line geometry. The focused Python suite passed 50 tests and all relevant frontend tests passed. A full JavaScript sweep still has the pre-existing unrelated OrbitWhisper-link assertion failure; a full pytest sweep must ignore the user-owned `backups/` snapshots because duplicate test filenames trigger collection mismatch.

- 2026-09-18 published: Commit `003bf49` was pushed to `origin/main`. GitHub Pages and `rocket.rainywhisper.com` now expose the 30-day date-window logic, and the published CSV still contains B1847/26, A0095/26, B4913/26, and B4610/26. Existing staged/unstaged MSI work and the user-owned `backups/` directory remain outside the commit.

- 2026-09-16 result: Added `NZZO` to the FAA supplemental FIR search, added Mahia/LC-1 aliases and canonical coordinate resolution, and made RLL context parsing use `win_open` when `t0` is null. The live API now yields `Owl By The Dozen` at 2026-09-19 03:15 UTC with LC-1 coordinates, and the backend retains the API as primary. Targeted Python tests, 31 supplemental tests excluding the intentionally slow logging case, the slow case separately, and nine frontend launch regressions passed. Live NOTAM search currently shows no issued Owl-specific Sep 19 notice; no unverified NOTAM was manually inserted. Risk: FAA's public search endpoint returned HTTP 403 during this audit, so actual NZZC/NZZO records will appear when an upstream source is available; next scheduled fetch should verify the issued notice.

- 2026-09-16: Live RocketLaunch.Live inspection returned `Owl By The Dozen` with `win_open=2026-09-19T03:15Z` but `t0=null`; the backend currently reads only `t0` and would therefore discard this API launch context. Extend the existing scoped resolver to use `win_open` as the API-provided fallback time, matching the frontend's current behavior and leaving the API record/source unchanged.

- 2026-09-16: Make the existing VV30 fallback regression assertion date-aware. `A1400/26` has now expired under the fetcher's rolling time filter while the other verified fallback records remain valid; the test should require the still-valid records and validate `A1400/26` only when it is within the live window.

- 2026-09-16: Audit New Zealand `Owl By The Dozen` (Rocket Lab Launch Complex 1, Mahia Peninsula, 2026-09-19 03:15 UTC). Live NOTAM search shows the recent Mahia launch pattern uses local `NZZC` danger-area notices plus downrange `NZZO` Auckland Oceanic notices; the code already queries `NZZC` but omits `NZZO`. The RocketLaunch.Live label `Rocket Lab Launch Complex, Mahia Peninsula` also does not exactly match the existing `launch_sites.csv` name, so the backend can fail to create a launch context. Planned scoped fix: add `NZZO`, add Mahia/LC-1 aliases and coordinate resolution, preserve the RocketLaunch.Live API as the primary source, and add regression coverage before publishing.

- 2026-09-16: Make the Shanghai Sea Launch regression test compatible with rolling NOTAM snapshots. A4352/26 is allowed to disappear from the current `notams.csv` after its validity window expires; if present, its supplied centre/time remain validated, while the synthetic warning below continues to validate launch matching. This prevents scheduled data cleanup from masking frontend regression results.
- 2026-09-16 result: Added `mergeUpcomingLaunches` in `index.html`. RocketLaunch.Live API records are kept first and unchanged; local fallback records are omitted only when a matching API launch or duplicate local fallback exists. Matching uses normalized mission names, a two-hour UTC tolerance, and normalized launch-site labels, so the live `Progress MS-35` record suppresses local `Progress MS-35 (96P)` while API-missing local launches remain available. Nine frontend regression tests now pass, including the new deduplication test. Risk: deliberately generic records such as `TBD` require both matching time and site to be considered duplicates. Next: publish this scoped deduplication change after preflight.
- 2026-09-16: Add source-aware upcoming-launch deduplication. Keep RocketLaunch.Live API records authoritative and use local manually curated records only when no matching API launch exists; match equivalent records using normalized mission names plus shared UTC time or launch location so suffix differences such as `Progress MS-35` versus `Progress MS-35 (96P)` do not create duplicate markers. Do not disable or replace the API feed.
- 2026-09-16: Fix launch-layer expiry handling so local and remote launch records are retained in the upcoming layer only while their parsed UTC launch time is strictly in the future. This covers manually location-adjusted fallback entries, including Shanghai Sea Launch and Sentinel-3C & FLEX, and prevents an elapsed scheduled time from remaining visible as an upcoming launch. Add regression tests for the filter and preserve historical launch data separately.
- 2026-09-16 result: Implemented `filterFutureLaunches` plus a scheduled expiry callback in `index.html`; elapsed records are removed on initial load and from an already-open page just after their UTC launch time. Verified the manually scheduled Shanghai and Sentinel-3C & FLEX records are hidden at a simulated 2026-09-16 01:30 UTC while the future Progress MS-35 record remains visible. Frontend regression suite passed; Python direct-entry regression checks passed, with `pytest` unavailable in the environment. Risk: launch feeds without a usable time remain visible as TBD by design. Next: publish the scoped frontend/test change to GitHub Pages.
- 2026-09-14: Add Guiana Space Centre / ELA-1 launch fallback for Sentinel-3C / Vega VV30. Extend launch-context FIR coverage to SOOO, TTZP, and KZWY so local and downrange aerospace NOTAMs are searched even when the remote launch feed omits the mission. Preserve the existing MSI and Baikonur changes.
- 2026-09-14: Add Vandenberg SFB / SLC-4E launch fallback for USSF-259. The remote launch feed omits the classified mission, while KZLA and KZAK are already in the supplemental FIR list; add local schedule context and explicit Vandenberg aliases so launch-correlated searches continue to cover the local and Pacific downrange regions. No current NOTAM was manually added because live KZLA rocket/airspace results did not identify a USSF-259 notice.
